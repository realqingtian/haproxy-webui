apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: haproxy-webui

resources:
  - 00-namespace.yaml
  - 10-secret.yaml
  - 20-pvc.yaml
  - 30-backend.yaml
  - 40-frontend.yaml

images:
  - name: haproxy-webui-backend
    newTag: dev
  - name: haproxy-webui-frontend
    newTag: dev
