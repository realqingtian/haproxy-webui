# 迭代路线图(Roadmap)

> 本文档只做**前瞻规划**,不是进度真源。每期开工时,把对应任务搬进根目录 `PLAN.md` 追加里程碑段,
> 进度以 PLAN.md 的复选框为准。任务状态:`[ ]` 未开工 / `[~]` 进行中 / `[x]` 完成。
>
> 规划原则:每期独立可交付;标注环境依赖(标注 ⚠️环境 的项需要用户提供对应条件才能验证,
> 无环境不排期);质量与体验优先于堆功能。

---

## v0.5 体验与质量(推荐下一期,纯本地可完成)

**目标**:把 M1–M4 已有能力打磨到"敢给团队日常用"的水平,不新增依赖环境。

- [ ] 配置批量暂存区(staging)
  - 现状:每保存一次配置就开一次事务 + 一次 reload,连续调整时 reload 频繁
  - 目标:编辑操作先进入"待提交清单"(跨对话框累积),页面常驻入口显示待提交数量;
    提交前展示操作列表摘要;一次事务批量应用,只触发一次 reload;支持逐条移除与一键清空
- [ ] 文本级 diff 预览
  - 现状:提交前只有字段级摘要
  - 目标:暂存区提交前,通过事务内 raw 配置与当前 raw 生成 unified diff 高亮展示(M3 已具备事务内读取能力)
- [ ] 后端自动化测试
  - dataplane 客户端单测:202/201/204 状态码语义、/v3 字段差异(weight 数字、name 字段)回归
  - BFF API 集成测试:复用 `deploy/dataplaneapi/local-e2e` 容器跑真实链路
- [ ] E2E 冒烟测试(Playwright)
  - 登录 → 实例增删 → 配置读取 → 运行时上下线 → 模板创建 → 回滚,覆盖核心链路
- [ ] 多实例体验
  - 侧边栏「配置管理」在多实例时弹出实例选择器(当前固定进第一个实例)
  - 监控聚合总览:单页展示全部实例健康状态(UP/DOWN 计数、QPS 汇总),点击下钻
- [ ] 小项打包:前端构建分包(消除 chunk 500k 警告)、列表分页、暗色模式

**验收**:连续做 5 处配置修改只触发 1 次 reload;`go test ./...` 与 E2E 冒烟进 Makefile;
两个以上实例时选择器正常工作。

## v0.6 运维与可观测

**目标**:从"管理配置"扩展到"运维保障",具备告警与巡检能力。

- [ ] 配置快照定时抓取:后台任务按周期(可配)自动同步节点配置为快照,漂移可见
- [ ] 告警通知:reload 失败、backend 全 DOWN、连通性探测失败时推送 Webhook
  (飞书 / 钉钉 / 企业微信机器人,可配置)
- [ ] 节点 Prometheus 探测:实例详情显示节点 8404 /metrics 是否可访问,未接入时给出接入指引
- [ ] 会话管理:JWT 刷新与主动吊销(改密 / 删除用户后强制下线)
- [ ] 审计日志增强:时间范围过滤、导出 CSV

**验收**:人为制造 reload 失败能收到通知;改密后旧 token 失效。

## v0.7 集群与生态(⚠️环境依赖,条件具备后排期)

- [ ] ⚠️环境:keepalived 主备集群视角(VIP 状态、主备节点健康一览),需节点实际部署 keepalived
      (降级第一阶段:集群分组模型 + UI 已于 M5-3 完成;剩余仅 VRRP 真实状态探测)
- [x] OIDC / SSO 登录(2026-09-17 提前至 M5 完成:标准 Authorization Code + PKCE,本地 dex 验证;
      上线仅需替换 IdP 环境变量)
- [x] k8s 部署清单(2026-09-17 提前至 M5 完成:deploy/k8s kustomize 清单,本机 kind 集群验证通过)
- [ ] dataplaneapi 服务管理:节点上 dataplaneapi 进程状态展示与远程重启
  (依赖 systemd 接口方案设计)

---

## 需求池(有价值但暂不排期)

- 配置搜索与跳转:在大配置中按 frontend/backend 名称定位
- SSL 证书管理:基于 dataplaneapi storage(ssl_certificates)上传与查看证书
- Runtime maps 在线编辑
- stats WebSocket 实时推送(替代 10s 轮询)
- 移动端 / 窄屏适配
- 多语言(i18n)
- 节点 haproxy 日志尾部查看(需节点侧日志接口方案)

---

## 已知技术债(随迭代顺手偿还)

| 债 | 影响 | 偿还时机 |
|---|---|---|
| dataplaneapi 以 root 运行 | 节点安全面较大 | v0.6+:普通用户 + polkit/sudo 白名单 |
| 实例凭据加密密钥缺省派生自 JWT secret | 换 JWT secret 会导致历史密文不可解 | v0.6:独立密钥必填校验 |
| 前端单 chunk >500kB | 首屏加载略慢 | v0.5 打包分包项 |
| local-e2e 容器内 haproxy 为 3.4、真实节点为 2.8 | 测试环境与生产行为有差异 | v0.6:e2e 镜像提供 2.8 tag |
